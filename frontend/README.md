# Stock Tracker web client

See the [project README](../README.md) for setup and external-service requirements.
Copy `.env.example` to `.env` before building. Browser environment variables are public in the generated bundle.
